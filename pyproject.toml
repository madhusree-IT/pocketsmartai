[project]
name = "pocketsmartai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "madhu sree", email = "madhuu.27777@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastapi[standard]>=0.128.8",
]

[project.scripts]
pocketsmartai = "pocketsmartai:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
